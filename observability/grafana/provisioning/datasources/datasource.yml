# Grafana datasource provisioning — Prometheus (uid khớp dashboard da10_api.json).
apiVersion: 1

datasources:
  - name: Prometheus
    uid: prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: true
