"""Scripts (entry points) for DA10 pipelines."""

# TODO(hieudm): add convenience CLI wrappers for the 3 pipelines
#   - scripts.clean_pipeline
#   - scripts.validation_pipeline
#   - scripts.dedup_pipeline
# Có thể dùng `python -m scripts.<name>` hoặc `click`/`typer` sau này.
