export const config = {
  API_BASE_URL: "http://localhost:8000",
  USE_MOCK_API: false,   // nối API backend thật (POST /search, /context)
  SEARCH_ENDPOINT: "/search",
  CONTEXT_ENDPOINT: "/context"
};

export default config;
